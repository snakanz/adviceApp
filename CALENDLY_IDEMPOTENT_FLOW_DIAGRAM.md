# 🔄 Calendly Idempotent Reconnect Flow — Complete Diagram

## FLOW: User Reconnects Calendly (Fully Idempotent)

```
┌─────────────────────────────────────────────────────────────────┐
│ USER CLICKS "RECONNECT CALENDLY" IN SETTINGS                   │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│ STEP 1: Check Existing Connection                              │
│ SELECT * FROM calendar_connections                             │
│ WHERE user_id = {USER_ID} AND provider = 'calendly'            │
└─────────────────────────────────────────────────────────────────┘
                              ↓
                    ┌─────────┴─────────┐
                    ↓                   ↓
            ✅ EXISTS              ❌ NOT EXISTS
                    ↓                   ↓
        ┌───────────────────┐  ┌──────────────────┐
        │ Try Token Refresh │  │ Full OAuth Flow  │
        └───────────────────┘  └──────────────────┘
                    ↓                   ↓
        ┌───────────────────┐  ┌──────────────────┐
        │ POST /oauth/token │  │ Redirect to      │
        │ grant_type:       │  │ Calendly OAuth   │
        │ refresh_token     │  │ (user authorizes)│
        └───────────────────┘  └──────────────────┘
                    ↓                   ↓
            ┌───────┴────────┐         │
            ↓                ↓         │
        ✅ SUCCESS      ❌ FAILED      │
            ↓                ↓         │
        SKIP OAUTH      PROCEED TO    │
            │            FULL OAUTH   │
            └────────────┬────────────┘
                         ↓
┌─────────────────────────────────────────────────────────────────┐
│ STEP 2: Get Calendly User Info                                 │
│ GET /users/me                                                   │
│ Returns: uri, email, current_organization                      │
└─────────────────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────────────┐
│ STEP 3: Update calendar_connections                            │
│ UPDATE calendar_connections SET                                │
│   access_token = NEW_TOKEN                                     │
│   refresh_token = NEW_REFRESH                                  │
│   calendly_user_uri = USER_URI                                 │
│   calendly_organization_uri = ORG_URI                          │
│   is_active = true                                             │
│ WHERE user_id = {USER_ID}                                      │
└─────────────────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────────────┐
│ STEP 4: Ensure Webhook Subscription                            │
│ GET /webhook_subscriptions?organization={ORG_URI}              │
└─────────────────────────────────────────────────────────────────┘
                         ↓
            ┌────────────┴────────────┐
            ↓                         ↓
    ✅ WEBHOOK EXISTS         ❌ NO WEBHOOK
            ↓                         ↓
    ┌──────────────────┐    ┌──────────────────┐
    │ Delete Old       │    │ Create New       │
    │ Webhook          │    │ Webhook          │
    │ (cleanup)        │    │ POST /webhook... │
    └──────────────────┘    └──────────────────┘
            ↓                         ↓
    ┌──────────────────┐    ┌──────────────────┐
    │ Create Fresh     │    │ Calendly Returns │
    │ Webhook          │    │ webhook_uri +    │
    │ POST /webhook... │    │ signing_key      │
    └──────────────────┘    └──────────────────┘
            ↓                         ↓
            └────────────┬────────────┘
                         ↓
┌─────────────────────────────────────────────────────────────────┐
│ STEP 5: Store Webhook in Database                              │
│ INSERT INTO calendly_webhook_subscriptions                     │
│   organization_uri = ORG_URI                                   │
│   webhook_subscription_uri = WEBHOOK_URI                       │
│   webhook_signing_key = SIGNING_KEY                            │
│   is_active = true                                             │
│                                                                 │
│ UPDATE calendar_connections SET                                │
│   calendly_webhook_id = WEBHOOK_URI                            │
│   calendly_webhook_signing_key = SIGNING_KEY                   │
│ WHERE user_id = {USER_ID}                                      │
└─────────────────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────────────┐
│ STEP 6: Trigger Initial Sync (Background)                      │
│ Fetch all existing Calendly meetings                           │
│ Store in meetings table with meeting_source = 'calendly'       │
└─────────────────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────────────┐
│ ✅ RECONNECTION COMPLETE                                        │
│ - Tokens refreshed or replaced                                 │
│ - Webhook cleaned up and recreated                             │
│ - Database in sync with Calendly                               │
│ - Ready for real-time webhook events                           │
└─────────────────────────────────────────────────────────────────┘
```

## KEY PROPERTIES (Idempotent)

✅ **Safe to run multiple times** - No duplicate webhooks created
✅ **Cleans up old state** - Deletes stale webhooks before creating new ones
✅ **Tokens always fresh** - Refresh attempted first, full OAuth fallback
✅ **Database consistent** - All tokens, URIs, and webhook IDs stored
✅ **No manual cleanup needed** - Automatic webhook management
✅ **Handles 409 Conflicts** - Gracefully recovers from existing webhooks

## ERROR HANDLING

```
Token Refresh Fails
  → Fall back to full OAuth ✅

Webhook Creation Fails (409 Conflict)
  → Fetch existing webhook ✅
  → Use existing webhook ✅

Webhook Deletion Fails
  → Log warning, continue ✅
  → Create new webhook anyway ✅

Database Update Fails
  → Rollback webhook creation ✅
  → Return error to user ✅
```

## DISCONNECT FLOW

```
User clicks "Disconnect Calendly"
  ↓
DELETE webhook from Calendly API
  ↓
Clear tokens from database
  ↓
Set is_active = false
  ↓
✅ Disconnected - no orphaned webhooks
```

